# My Baby Gender Predictor Validator

A lightweight FastAPI service that validates whether uploaded images look like clear ultrasound scans.

### Run locally
```bash
pip install -r requirements.txt
uvicorn main:app --reload --port 8000



cat > main.py << 'EOF'


